#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
test -e config/zendesk.yml || cp config/zendesk.yml.example config/zendesk.yml
